#include "holberton.h"
#include <string.h>
/**
 * _strlen - finding the length
 * @s: string going in
 * Return: int of how long string is
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; *s != 0; i++, s++)
	{
	}
	return(i);
}
