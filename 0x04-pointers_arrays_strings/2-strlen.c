#include "holberton.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - finding the length
 * @s: string going in
 * Return: int of how long string is
 */
int _strlen(char *s)
{
	return (strlen(s));
}
