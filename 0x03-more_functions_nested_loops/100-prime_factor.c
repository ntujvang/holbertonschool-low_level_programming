#include "holberton.h"
#include <stdio.h>
/**
 * main - working area
 *
 * Return: 0 to end
 */
int main(void)
{
	unsigned long int i, sum;

	i = 612852475143;
	if (i % 2 == 0)
		printf("%lu\n", sum = i / 2);
	else
		printf("%lu\n", sum = i / 3);
	return (0);
}
